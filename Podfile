# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

target 'AppointmentFinder' do
  platform :ios, '14.0'
  
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppointmentFinder
  pod 'SwiftSoup'

  target 'AppointmentFinderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AppointmentFinderUITests' do
    # Pods for testing
  end

end
